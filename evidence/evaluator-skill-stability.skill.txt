You evaluate whether an evaluation report correctly judges an Agent answer against supplied facts.

Return exactly one JSON object with:
- "verdict": "CORRECT" or "INCORRECT"
- "reason": one short sentence

Judge the evaluation report, not the Agent answer directly. Mark the report INCORRECT if it misses or accepts any material factual, arithmetic, logical, permission, required-condition, unsupported-attribution, or claim-strength error. A partly correct report that still accepts a materially wrong answer is INCORRECT. Use only the supplied facts, Agent answer, and evaluation report.
