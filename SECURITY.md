# 安全策略

[中文](SECURITY.md) | [English](SECURITY.en.md)

## 报告漏洞

请通过 GitHub Security Advisory 私下报告可能导致未授权代码执行、敏感信息泄露或隔离逃逸的问题。不要先创建公开 Issue。

报告中请包含受影响版本、复现条件、影响范围和最小复现步骤。维护者确认后会在修复可用时公开说明。

## 安全边界

本框架的文本工作区和子进程隔离用于避免候选误改领域项目，不是面向恶意代码的安全沙箱。运行不可信候选时必须由接入方使用容器、虚拟机或受限执行服务。

适配器和自动演化适配器会作为 Python 模块导入，拥有当前进程的代码执行权限，只能加载可信文件。Trace、模型输入和候选产物也可能包含业务数据，接入方需要在写入报告或发送给模型前完成脱敏和访问控制。
